import { TalentFilterPipe } from './talent-filter.pipe';

describe('TalentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TalentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
